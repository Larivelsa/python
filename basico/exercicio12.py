'''
Complete o código a seguir levando em consideração os seguintes itens:

- Deve ser criada uma variável com o nome "contador", e deve ser iniciada com o valor 0;
- Use o laço de repetição estrutura de repetição while para imprimir a variável contador a cada iteração;
- O loop while deve ser executado enquanto a variável "contador" for menor que 5; (A condição deve ser exatamente essa)
- A cada iteração, a variável "contador" deve receber o valor dela somado com o valor 1.

'''


contador = 0
while (contador<5):
   print(contador)
   contador+=1
